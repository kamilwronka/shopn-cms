{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "uri": "${process.env.CMS_MONGODB_URI || ''}"
      },
      "options": {
        "ssl": true
      }
    }
  }
}
