{
  "host": "${process.env.CMS_HOST || '0.0.0.0'}",
  "port": "${process.env.CMS_PORT || 1337}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
